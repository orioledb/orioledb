#!/bin/sh
set -eu

# This container only hosts test commands for Antithesis to invoke
# (see /opt/antithesis/test/v1/sk-rebuild-desync/); it has no long-running
# process of its own, so just stay alive.
exec sleep infinity
