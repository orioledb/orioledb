#!/bin/sh
set -eu

# Under Antithesis, sleep indefinitely: Antithesis runs the test template
# script. Locally, run the test template script.
if [ -n "${ANTITHESIS_OUTPUT_DIR:-}" ]; then
	exec sleep infinity
fi

exec /opt/antithesis/test/v1/sk-recovery-race-chaos/singleton_driver_sk-recovery-race-chaos "$@"
